<h1>User info</h1>
 <div>
   <%= image_tag @user.get_profile_image(100, 100) %>
   <b>name</b>
   <%= @user.name %>
   <b>introduction</b>
   <%= @user.introduction %>
 </div>

<h1>Users</h1>
<% @books.each do |book| %>
  <div>
    <b>image<%= image_tag book.user.get_profile_image(100,100) %></b>
    <b>name<%= book.title %></b>
  </div>
<% end %>
